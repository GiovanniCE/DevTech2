services:
  db1:
    image: mysql/mysql-server:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: cs506
      MYSQL_DATABASE: BoatsAndSailors
      MYSQL_ROOT_HOST: '%'
    volumes:
    - basdat1:/var/lib/mysql
    ports:
    - 53316:3306

volumes:
  basdat1:
